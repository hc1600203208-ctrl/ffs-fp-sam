# New Python packages installed in `sam`

Installed with:

```bash
~/anaconda3/bin/conda run -n sam pip install trimesh pyrender PyOpenGL imageio
```

Added packages:

- `trimesh`
- `pyrender`
- `PyOpenGL`
- `imageio`

