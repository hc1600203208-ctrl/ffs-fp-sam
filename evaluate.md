现在我有一套基于fast foundation stereo进行双目深度估计，使用bundlesdf进行三维重建，使用foundationpose进行实时位姿跟踪的实验栈，我在原始算法上做了三点改进，在fast foundation stereo基础上加了深度置信度估计，并滤出低置信度深度点，提高后续重建质量；在bundlesdf基础上加入了黑色空洞补全，提高mesh模型质量；在founationpose加入了质量滤波算法，提高动态鲁棒性。我已经在一组数据上跑通了整套流程，并进行了三个改进的评估实验。对于一组数据，以/home/hc/weizi/dataset/jrnew-blue为例，需要包含左相机图像/rgb，右相机图像/camera2，物体掩模/masks，左相机内参cam_K.txt,双目标定参数文件cam_info.txt;实验流程如下：先跑/home/hc/weizi/ffs+fp+sam/ffs/src/ffs_pkg/launch/offline_depth_benchmark.launch.py脚本生成不同参数设置的置信度估计算法得到多组深度图。bundlesdf程序在/home/hc/weizi/BundleSDF下，有两个分支，当前分支gaijin是加入了空洞补全的版本，而main分支是没有加空洞补全的版本；conda安装位置为~/anaconda3，首先在conda环境名为bundlesdf下，跑main分支的/home/hc/weizi/BundleSDF/batch_depth_benchmark.py对多组深度图分别进行三维重建得到mesh模型，并评估不同参数设置的置信度估计算法的重建质量；然后选取其中综合重建质量最好的一组，切换到gaijin分支进行bundlesdf空洞补全实验，再对加入空洞补全的mesh模型进行评估，和加入前的mesh模型的质量进行对比；最后在fp_ros虚拟conda环境下评估foudnationpose跟踪性能，foundationpose主离线运行程序在/home/hc/weizi/FoundationPoseROS2下，/home/hc/weizi/FoundationPoseROS2/FoundationPose/run_demo_adaptive_filter.py是加入滤波后的版本，/home/hc/weizi/FoundationPoseROS2/FoundationPose/run_demo.py是原始版本，使用/home/hc/weizi/FoundationPoseROS2/evaluate_pose_tracking_no_gt.py脚本自动跑两个版本的程序并对跟踪输出位姿结果进行评估，mesh模型使用前面得到的重建质量最好的一组深度图根据是否进行空洞补全，分别得到的两组mesh；然后两组mesh都用两个版本的foundationpose进行实验和评估；foundationpose的评估数据使用和重建的同一物体的的另一组rgbd图像数据序列，这里脚本应该留有指定数据文件夹的位置，这里的数据文件夹应该至少包括左相机图像/rgb，深度图/depth，物体掩模/masks，左相机内参cam_K.txt。将所有的评估结果和三维重建的中间结果保存在数据同级目录evaluate下，例如/home/hc/weizi/dataset/jrnew-blue/evaluate。
请编写一个脚本能够对批量的上面提到的格式的双目数据集完成上面的所有任务，脚本存放在/home/hc/weizi/ffs+fp+sam/ffs/src/ffs_pkg/scripts